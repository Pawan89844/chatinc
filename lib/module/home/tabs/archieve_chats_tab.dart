import 'package:flutter/material.dart';

class ArchieveChatsTab extends StatelessWidget {
  const ArchieveChatsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      margin: EdgeInsets.only(top: 12.0),
      child: Center(child: Text('Archieve tab')),
    );
  }
}
